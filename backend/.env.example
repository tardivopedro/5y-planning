# Copie este arquivo para backend/.env e ajuste os valores conforme o ambiente.

# Banco padrão em desenvolvimento local: utilize o host público do Railway
# (Settings → Connect → Public) ou outro Postgres à sua escolha.
# Para desenvolvimento local usando o proxy público da Railway (substitua host/porta):
# DATABASE_URL=postgresql+psycopg://postgres:<senha>@shortline.proxy.rlwy.net:10834/railway?sslmode=require

# Para serviços hospedados NA Railway use o host interno:
# DATABASE_URL=postgresql+psycopg://postgres:<senha>@postgres.railway.internal:5432/railway

# ATENÇÃO: o host `postgres.railway.internal` só funciona dentro da própria infraestrutura do Railway.
# Para desenvolvimento local, use o host público acima ou deixe comentado para cair no SQLite.
# DATABASE_URL=postgresql+psycopg://postgres:WjxaxsOPvSLvflWokyKqYvGFhQpEoabY@postgres.railway.internal:5432/railway

# Caso queira ficar no SQLite local, basta manter essa linha comentada e o backend
# voltará a usar o arquivo data/forecast.db.

# Token exigido para limpeza da base
DELETE_CONFIRMATION_TEXT=DELETE-ALL
